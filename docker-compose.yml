version: '3.8'

services:
  detector:
    build: .
    container_name: anti-spoofing-detector
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

